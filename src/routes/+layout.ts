// This forces SvelteKit into Single Page Application (SPA) / Static Site Generation (SSG) mode.
// It generates a single static HTML shell that is perfectly suited for offline caching.
export const prerender = true;
export const ssr = false;
