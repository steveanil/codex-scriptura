/**
 * Milestone labels the UI mentions ("arrives with v0.5.0"). One place, so
 * a renumbered milestone changes every empty state at once; keep in step
 * with the GitHub milestones.
 */
export const MILESTONES = {
    manuscripts: 'v0.5.0',
    resources: 'v0.6.0',
    plugins: 'v1.1.0',
} as const;
